# используйте "kubectl get pods" для получения обзора всех pod-ов и их текущего состояния
# более продвинутый мониторинг pod-а должен происходить с точки зрения мониторинга приложений: мониторьте не отдельный pod, а deployment, частью которого он является
# advisor - более продвинутый агент мониторинга, интегрированный с Kubelet для получения данных о производительности компонентов K8s
# prometheus - более продвинутое решения для мониторинга, которое позволят вам мониторить pod-ы также как ноды

kubectl get pods -n production   #смотреть поды в определенном namespace
kubectl get pods -o wide   #больше полей в выводе
kubectl get pods --show-labels -A   #смотреть labels pod-ов, -A=all-namespaces
