# по умолчанию логи пишутся в сам контейнер
# используйте docker run --rm -v /dev/log:/dev/log/ fedora:latest logger "message from container" для захвата логов контейнера в ОС хоста
# введите journalctl | grep container чтобы увидеть тестовое лог-сообщение на хосте с контейнером