# Probes - это часть спецификации контейнера и могут быть использованы для тестирования доступа к pod-ам
# readinessProbe (проба на готовность) используется, чтобы убедиться, что pod не опубликован как доступный до тех пор пока readinessProbe сможет получить доступ к нему
# livenessProbe (проба на живость) используется для продолжения проверки доступности pod-а
# Probe - это простой тест, который проверяет, что приложение отвечает

# следующие типы Probe определены в pods.container.spec:
# - exec: команда выполняется и возвращает нулевое exit value
# - httpGet: HTTP запрос возвращает код ответа в диапазоне между 200 и 399
# - tcpSocket: возможность подключения к TCP сокету (доступному порту) успешна

kubectl create -f busybox-ready.yaml ; kubectl get pods -w
# не все параметры pod-а можно изменить "налету" с помощью команды "kubectl edit pod"