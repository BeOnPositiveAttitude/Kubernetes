# pod - это некая абстракция сервера, если раньше приложения запускались на серверах, то теперь в pod-ах
# pod может нести в себе несколько контейнеров в пределах одного Namespace, опубликованных на одном IP-адресе
# важно запомнить, мы всегда обращаемся к pod-у, НЕ к контейнеру
# pod - минимальная сущность, которая может управляться с помощью K8s
# с точки зрения контейнера pod - это сущность, которая запускает один и более контейнеров, используя образ контейнера
# как правило pod-ы запускаются только через Deployment, т.к. "голые" pod-ы не могут быть перенесены в случае падения ноды

# используйте "kubectl run" для запуска deployment основанного на образе по умолчанию
kubectl run ghost --image=ghost:0.9
kubectl run nginx-test --image=nginx -o yaml   #создать pod и вывести сразу в YAML
# используйте kubectl объединенный с инструкциями из YAML-файла для выполнения необходимых вам действий
kubectl create -f <name>.yaml
kubectl get pods [-o yaml]   #покажет pod-ы в формате YAML
kubectl describe pods   #покажет все детали о pod, включая информацию о запущенных внутри контейнерах
kubectl describe pods newhttpd   #пример
kubectl edit pods mypod   #позволяет редактировать живые pod-ы