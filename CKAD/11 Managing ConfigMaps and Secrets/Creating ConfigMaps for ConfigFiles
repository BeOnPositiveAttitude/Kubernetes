# ConfigMaps также могут использоваться для предоставления полных конфигурационных файлов
# в этом случае pod использует ConfigMap как смонтированный volume, который содержит только конфигурационный файл
# и сама ConfigMap содержит конфигурационный файл
